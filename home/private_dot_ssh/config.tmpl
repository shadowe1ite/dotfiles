Host server
      Hostname server.{{ .domain }}
      ProxyCommand cloudflared access ssh --hostname %h
      User elite

Host pi
      Hostname pi.{{ .domain }}
      ProxyCommand cloudflared access ssh --hostname %h
      User {{ .user }}

Host pi.local
      Hostname 192.168.1.154
      User {{ .user }}
